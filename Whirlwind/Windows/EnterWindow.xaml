<Window x:Class="Whirlwind.EnterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Whirlwind"
        mc:Ignorable="d"
        Title="Enter Window"
        Height="160"
        Width="260"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Closing="Window_Closing"
        ContentRendered="Window_ContentRendered"
        KeyDown="enter_text_KeyDown"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Стек из 4 ячеек IP -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0">

            <!-- Ячейка 1 -->
            <TextBox x:Name="ip1"
                     MinWidth="40"
                     MaxLength="3"
                     PreviewTextInput="IpBox_PreviewTextInput"
                     TextChanged="IpBox_TextChanged"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     FontSize="16"/>

            <TextBlock Text="." FontSize="18" Margin="5 0" VerticalAlignment="Center"/>

            <!-- Ячейка 2 -->
            <TextBox x:Name="ip2"
                     MinWidth="40"
                     MaxLength="3"
                     PreviewTextInput="IpBox_PreviewTextInput"
                     TextChanged="IpBox_TextChanged"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     FontSize="16"/>

            <TextBlock Text="." FontSize="18" Margin="5 0" VerticalAlignment="Center"/>

            <!-- Ячейка 3 -->
            <TextBox x:Name="ip3"
                     MinWidth="40"
                     MaxLength="3"
                     PreviewTextInput="IpBox_PreviewTextInput"
                     TextChanged="IpBox_TextChanged"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     FontSize="16"/>

            <TextBlock Text="." FontSize="18" Margin="5 0" VerticalAlignment="Center"/>

            <!-- Ячейка 4 -->
            <TextBox x:Name="ip4"
                     MinWidth="40"
                     MaxLength="3"
                     PreviewTextInput="IpBox_PreviewTextInput"
                     TextChanged="IpBox_TextChanged"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     FontSize="16"/>
        </StackPanel>

        <Button x:Name="enter_button"
                Grid.Row="1"
                Click="enter_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" Height="35" Width="251">
            использовать этот ip
        </Button>

    </Grid>
</Window>
