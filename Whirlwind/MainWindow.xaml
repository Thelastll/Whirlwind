<Window x:Class="Whirlwind.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Whirlwind"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ContentRendered="Window_ContentRendered">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="33*"/>
            <ColumnDefinition Width="67*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#2B2B2B" Grid.Column="0">
            <DockPanel Margin="10">

                <Grid DockPanel.Dock="Top" Margin="0 0 0 10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="add_user"
            Content="+"
            Grid.Column="0"
            MinWidth="50"
            MinHeight="35"
            Background="#4A4A4A"
            Foreground="White"
            FontSize="20"
            BorderThickness="0"
            Margin="0 0 10 0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Click="add_user_Click"/>

                    <Button x:Name="change_sender_ipaddress"
            Content="Изменить IP адрес"
            Grid.Column="1"
            MinHeight="35"
            Background="#3A3A3A"
            Foreground="White"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Click="change_sender_ipaddress_Click"/>
                </Grid>

                <ListBox x:Name="DeviceList"
         DockPanel.Dock="Top"
         Background="#333"
         Foreground="White"
         BorderThickness="0"
         Margin="0 0 0 10"
         HorizontalAlignment="Stretch"
         VerticalAlignment="Stretch"
         HorizontalContentAlignment="Stretch">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10"
                    Margin="0 0 0 5"
                    Background="#3A3A3A"
                    HorizontalAlignment="Stretch"
                    MouseLeftButtonUp="device_item_Click">

                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Обновить"
                                  Click="update_device_Click"/>
                                        <MenuItem Header="Удалить"
                                  Click="delete_device_Click"/>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <StackPanel>

                                    <TextBlock Text="{Binding HiddenId}"
                               Visibility="Collapsed"/>

                                    <TextBlock Text="{Binding Name}"
                               FontSize="16"
                               Foreground="White"/>

                                    <TextBlock Text="{Binding Ip}"
                               FontSize="12"
                               Foreground="#CCCCCC"/>

                                </StackPanel>

                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <StackPanel Background="#3A3A3A">
                <TextBlock x:Name="ChatTitle"
                       Text="_"
                       FontSize="18"
                       Background="#3A3A3A"
                       Foreground="White"
                       Margin="10 0 10 0"/>
                <TextBlock x:Name="IpTitle"
                       Text="_"
                       FontSize="14"
                       Background="#3A3A3A"
                       Foreground="#CCCCCC"
                       Margin="10 0 10 0"/>
            </StackPanel>

            <ScrollViewer x:Name="MessagesScroll"
                        Grid.Row="1"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="MessagesPanel"
                        Margin="0 5 0 10"/>

            </ScrollViewer>

            <Grid Grid.Row="2" Margin="0 10 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="message"
                         Grid.Column="0"
                         MinHeight="40"
                         MinWidth="200"
                         FontSize="16"
                         Background="#2B2B2B"
                         Foreground="White"
                         BorderThickness="1"
                         BorderBrush="#555"
                         Margin="0 0 10 0"
                         VerticalAlignment="Center"
                         TextWrapping="Wrap"/>

                <Button x:Name="send_button"
                        Grid.Column="1"
                        Content="→"
                        MinWidth="60"
                        MinHeight="40"
                        Background="#007ACC"
                        Foreground="White"
                        FontSize="20"
                        BorderThickness="0"
                        VerticalAlignment="Center"
                        Click="send_button_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
